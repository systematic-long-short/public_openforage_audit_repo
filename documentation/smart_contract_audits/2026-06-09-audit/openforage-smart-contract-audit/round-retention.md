# Round Retention Note

This audit package is the durable artifact set for the 2026-06-09
`/openforage-smart-contract-audit` campaign. The package retains the current
full audit artifacts, command logs, regression red/green logs, and final
validation logs under this dated directory and `_audit_work/logs/`.

The numbered Codex entries in `codex-review.md` are adversarial review
iterations. They are retained as review records in that file; they did not each
produce a separate copied `_audit_work/` directory. No retroactive per-iteration
artifact directories were fabricated after the phase-7 reviewer raised this
ambiguity.

If A2 is interpreted as "each full audit-run artifact set must be retained," the
current dated package is the retained source of truth for the campaign. If A2 is
interpreted as "every Codex adversarial reply must have its own full raw artifact
directory," that stricter evidence does not exist for the earlier iterations.
